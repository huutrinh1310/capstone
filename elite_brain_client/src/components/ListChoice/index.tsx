import { Choice } from '@/interface/Choice.interface';
import * as React from 'react';

export interface ListChoiceProps {
    listChoice: any;
}

export default function ListChoice({ listChoice }: ListChoiceProps) {
    return <div></div>;
}
